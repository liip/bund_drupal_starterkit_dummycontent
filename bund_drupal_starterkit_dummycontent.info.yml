type: module
name: Bund Drupal Starterkit Dummy Content
description: 'Imports dummy contents from CSV files to have an exemple of how a Federal website looks like.'
package: Migration
core: 8.x
dependencies:
  - migrate
  - migrate_plus
  - migrate_source_csv
config_devel:
  install:
    - migrate_plus.migration.menu_item
    - migrate_plus.migration_group.bund
